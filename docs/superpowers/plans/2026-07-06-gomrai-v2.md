# Gom rải v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thay trigger cờ "Gom rải" (hiện: level `cycleProb≥60`, quá dày + bỏ đáy) bằng trigger tuyển chọn qua study 2 giai đoạn + placebo; NO-GO ⇒ xóa cờ khỏi UI.

**Architecture:** 1 script study offline trên data commit quyết định GO/NO-GO + cấu hình thắng. GO ⇒ `GOMRAI_CONFIG` + hàm thuần edge/TTL/cooldown trong `src/lib/timeline.ts`, Dashboard live và Time Machine cùng gọi (chart ≡ card). NO-GO ⇒ gỡ level `dca` + dải. Không đụng composite/presets/Bear DCA/Bear Downside/Accumulation/stats `bottom.json`.

**Tech Stack:** TypeScript, Next.js static, vitest, tsx cho scripts.

**Spec:** `docs/superpowers/specs/2026-07-06-gomrai-v2-design.md`

## Global Constraints

- **KHÔNG `git commit` / `git push`** — rule toàn cục của chủ dự án. Cuối mỗi task chỉ báo "sẵn sàng commit".
- UI text tiếng Việt.
- Study chỉ đọc data đã commit (`public/data/*.json`), không fetch mạng.
- Không đụng: composite, presets v4, consensus, Bear DCA, Bear Downside, Accumulation brake, backtest buckets, cách tính `prob`/`ci`/`calibration` của `bottom.json`.
- Giữ 2 chính sách đã chốt: loại ngày headwind (radar composite ≤ −40); pha Bear DCA `acute` ⇒ prob rớt về `probUnweighted`.
- Luật "dải trên chart ≡ card guidance từng ngày" (golden test) phải giữ.

---

### Task 1: Study script `scripts/gomrai-study.ts`

**Files:**
- Create: `scripts/gomrai-study.ts`

**Interfaces:**
- Consumes: `public/data/timeline.json` (dense points: `date, price, composite, cycleBin, swingBin, cycleProb, cycleProbUw, cycleN, swingProb, swingProbUw, swingN, returns["126"]`), `public/data/bottom.json` (`confirmedBottoms: {date, price, tier}[]`), `bearPhases(prices)` từ `src/lib/bear-dca.ts`.
- Produces: bảng console — hiện trạng cờ cũ (reference), grid kết quả, danh sách cấu hình qua cổng xếp theo min-excess, phán quyết GO/NO-GO. Dòng WINNER in JSON cấu hình để Task 3 copy.

Đây là research code — không unit test (đồng quy ước mọi `*-study.ts` trong repo). Verification = chạy + checklist sanity.

- [ ] **Step 1: Viết script**

```ts
/**
 * GOM RẢI v2 — tuyển chọn trigger cờ "Gom rải" (spec 2026-07-06-gomrai-v2-design.md).
 * Cờ hiện tại (level cycleProb≥60) đo được: 430 ngày bật / 48 đợt, riêng 2026 54 ngày,
 * nhưng chỉ 35/194 đáy xác nhận có cờ trong ±7 phiên (18%) — vừa dày vừa bỏ đáy.
 *
 * Grid: trigger CẠNH LÊN {bin cycle/swing/union · prob cycle/swing/max ≥ thr · lai}
 *       × TTL {0,2,3,5,8,10} × cooldown {0,10,21,42}.
 * Cổng: (G1) win6T per-đợt > baseline ở CẢ train(<2019)/test(≥2019)
 *       (G2) ≥ p90 placebo đồng-n (500 seed, random ngày đủ điều kiện cùng năm)
 *       (G3) coverage đáy gộp ≥35% · ngày active/năm ≤25 · bắn ≥60% số năm.
 * Xếp hạng min-excess (lợi thế win6T tệ nhất trong 2 giai đoạn).
 * Chính sách giữ nguyên: loại ngày headwind (composite ≤ −40); pha acute ⇒ probUw.
 * Offline data commit · walk-forward (prob/bin as-of có sẵn trong timeline) · seed cố định.
 * Chạy: npx tsx scripts/gomrai-study.ts
 */
import { readFileSync } from "node:fs";
import { bearPhases } from "../src/lib/bear-dca";
import type { BearPhase } from "../src/lib/types";

const SPLIT = "2019-01-01";
const WARMUP = 756;
const STEP = 3; // lưới thưa cho baseline (chống pseudo-replication)
const MINN = 10;
const NEAR = 7; // ±7 phiên quanh đáy xác nhận
const SEEDS = 500;

type Pt = {
  date: string; price: number; composite: number;
  cycleBin?: number; swingBin?: number;
  cycleProb?: number | null; cycleProbUw?: number | null; cycleN?: number;
  swingProb?: number | null; swingProbUw?: number | null; swingN?: number;
  returns: Record<string, number | null>;
};
const tl = JSON.parse(readFileSync("public/data/timeline.json", "utf8"));
const bot = JSON.parse(readFileSync("public/data/bottom.json", "utf8"));
const P: Pt[] = tl.points;
const N = P.length;
const dates = P.map((p) => p.date);
const phases: BearPhase[] = bearPhases(P.map((p) => p.price));
const dateIdx = new Map(dates.map((d, i) => [d, i]));

// ngày đủ điều kiện: sau warmup + không headwind (chính sách precedence của guidance)
const eligible = (i: number) => i >= WARMUP && P[i].composite > -40;

// prob hiệu dụng theo tầng, cổng acute (cùng công thức gomRaiIdxsBy hiện tại)
function effProb(i: number, tier: "cycle" | "swing"): number | null {
  const p = P[i];
  const rec = tier === "cycle" ? p.cycleProb ?? null : p.swingProb ?? null;
  const uw = tier === "cycle" ? p.cycleProbUw ?? null : p.swingProbUw ?? null;
  const n = tier === "cycle" ? p.cycleN ?? 0 : p.swingN ?? 0;
  if (n < MINN) return null;
  return phases[i] === "acute" ? uw ?? rec : rec;
}

// ---- nguồn điều kiện (mỗi nguồn tự có cạnh lên riêng; trigger = BẤT KỲ nguồn nào có cạnh)
type Src = (i: number) => boolean;
const binSrc = (tier: "cycle" | "swing"): Src => (i) =>
  i >= 0 && (tier === "cycle" ? P[i].cycleBin : P[i].swingBin) === 3;
const probSrc = (tier: "cycle" | "swing" | "max", thr: number): Src => (i) => {
  if (i < 0) return false;
  const v =
    tier === "max"
      ? Math.max(effProb(i, "cycle") ?? -1, effProb(i, "swing") ?? -1)
      : effProb(i, tier) ?? -1;
  return v >= thr;
};

interface Cand { name: string; srcs: Src[]; }
const THRS = [55, 60, 65, 70, 75];
const CANDS: Cand[] = [
  { name: "bin-cycle", srcs: [binSrc("cycle")] },
  { name: "bin-swing", srcs: [binSrc("swing")] },
  { name: "bin-union", srcs: [binSrc("cycle"), binSrc("swing")] },
  ...(["cycle", "swing", "max"] as const).flatMap((t) =>
    THRS.map((thr) => ({ name: `prob-${t}-${thr}`, srcs: [probSrc(t, thr)] }))
  ),
  ...THRS.map((thr) => ({
    name: `hybrid-${thr}`,
    srcs: [binSrc("cycle"), binSrc("swing"), probSrc("max", thr)],
  })),
];
const TTLS = [0, 2, 3, 5, 8, 10];
const COOLS = [0, 10, 21, 42];

// ---- trigger + active window cho 1 cấu hình
function runConfig(c: Cand, ttl: number, cool: number) {
  const triggers: number[] = [];
  let last = -1e9;
  for (let i = WARMUP; i < N; i++) {
    if (!eligible(i)) continue;
    if (i - last <= cool) continue;
    if (c.srcs.some((s) => s(i) && !s(i - 1))) { triggers.push(i); last = i; }
  }
  const inWin = new Uint8Array(N);
  for (const t of triggers) for (let j = t; j <= Math.min(N - 1, t + ttl); j++) inWin[j] = 1;
  const active: number[] = [];
  for (let i = 0; i < N; i++) if (inWin[i] && eligible(i)) active.push(i);
  return { triggers, active };
}

// ---- metric helpers
const inEra = (d: string, era: "train" | "test") => (era === "train" ? d < SPLIT : d >= SPLIT);
const median = (a: number[]) => { const s = [...a].sort((x, y) => x - y); return s.length ? s[Math.floor(s.length / 2)] : NaN; };
const winRate = (a: number[]) => (a.length ? a.filter((r) => r > 0).length / a.length : NaN);

// đáy xác nhận sau warmup, map về index
const bottoms = (bot.confirmedBottoms as { date: string; tier: string }[])
  .map((b) => ({ ...b, i: dateIdx.get(b.date) ?? -1 }))
  .filter((b) => b.i >= WARMUP);

function coverage(active: number[], tier?: string) {
  const set = new Set(active);
  const bs = tier ? bottoms.filter((b) => b.tier === tier) : bottoms;
  let hit = 0;
  for (const b of bs) {
    for (let d = -NEAR; d <= NEAR; d++) if (set.has(b.i + d)) { hit++; break; }
  }
  return bs.length ? (100 * hit) / bs.length : NaN;
}
function precision(active: number[]) {
  if (!active.length) return NaN;
  const bidx = bottoms.map((b) => b.i);
  let near = 0;
  for (const i of active) if (bidx.some((j) => Math.abs(j - i) <= NEAR)) near++;
  return (100 * near) / active.length;
}

// baseline: lưới thưa STEP=3, ngày đủ điều kiện, có returns 126
const baseRets: Record<"train" | "test", number[]> = { train: [], test: [] };
for (let i = WARMUP; i < N; i += STEP) {
  if (!eligible(i)) continue;
  const r = P[i].returns["126"];
  if (r == null) continue;
  baseRets[inEra(dates[i], "train") ? "train" : "test"].push(r);
}

// PRNG seed cố định (mulberry32) cho placebo
function mulberry32(seed: number) {
  return () => {
    seed |= 0; seed = (seed + 0x6d2b79f5) | 0;
    let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
// pool ngày đủ điều kiện có returns126 theo năm (cho placebo đồng-n)
const poolByYear = new Map<string, number[]>();
for (let i = WARMUP; i < N; i++) {
  if (!eligible(i) || P[i].returns["126"] == null) continue;
  const y = dates[i].slice(0, 4);
  (poolByYear.get(y) ?? poolByYear.set(y, []).get(y)!).push(i);
}
/** p90 win6T của placebo: giữ nguyên số trigger/năm, ngày random trong pool cùng năm. */
function placeboP90(trigsWithRet: number[], era: "train" | "test"): number {
  const perYear = new Map<string, number>();
  for (const i of trigsWithRet) {
    if (!inEra(dates[i], era)) continue;
    const y = dates[i].slice(0, 4);
    perYear.set(y, (perYear.get(y) ?? 0) + 1);
  }
  if (![...perYear.values()].some((c) => c > 0)) return NaN;
  const wins: number[] = [];
  for (let s = 0; s < SEEDS; s++) {
    const rnd = mulberry32(1000 + s);
    const rets: number[] = [];
    for (const [y, cnt] of perYear) {
      const pool = poolByYear.get(y) ?? [];
      for (let k = 0; k < cnt && pool.length; k++) {
        rets.push(P[pool[Math.floor(rnd() * pool.length)]].returns["126"]!);
      }
    }
    if (rets.length) wins.push(winRate(rets));
  }
  wins.sort((a, b) => a - b);
  return wins.length ? 100 * wins[Math.floor(0.9 * wins.length)] : NaN;
}

interface Row {
  key: string; cand: string; ttl: number; cool: number;
  trigN: number; activeN: number; years: number; yearsFired: number;
  covAll: number; covCycle: number; covSwing: number; prec: number;
  win: Record<"train" | "test", number>; med: Record<"train" | "test", number>;
  n: Record<"train" | "test", number>; minExcess: number; actPerYear: number;
  trigsWithRet: number[];
}
const spanYears = Number(dates[N - 1].slice(0, 4)) - Number(dates[WARMUP].slice(0, 4)) + 1;
const rows: Row[] = [];
for (const c of CANDS) for (const ttl of TTLS) for (const cool of COOLS) {
  const { triggers, active } = runConfig(c, ttl, cool);
  if (!triggers.length) continue;
  const trigsWithRet = triggers.filter((i) => P[i].returns["126"] != null);
  const win = { train: NaN, test: NaN } as Row["win"];
  const med = { train: NaN, test: NaN } as Row["med"];
  const n = { train: 0, test: 0 } as Row["n"];
  for (const era of ["train", "test"] as const) {
    const rets = trigsWithRet.filter((i) => inEra(dates[i], era)).map((i) => P[i].returns["126"]!);
    win[era] = 100 * winRate(rets); med[era] = median(rets); n[era] = rets.length;
  }
  const minExcess = Math.min(
    win.train - 100 * winRate(baseRets.train),
    win.test - 100 * winRate(baseRets.test)
  );
  rows.push({
    key: `${c.name}/ttl${ttl}/cd${cool}`, cand: c.name, ttl, cool,
    trigN: triggers.length, activeN: active.length,
    years: spanYears, yearsFired: new Set(triggers.map((i) => dates[i].slice(0, 4))).size,
    covAll: coverage(active), covCycle: coverage(active, "cycle"), covSwing: coverage(active, "swing"),
    prec: precision(active), win, med, n, minExcess,
    actPerYear: active.length / spanYears, trigsWithRet,
  });
}

// ---- reference: cờ HIỆN TẠI (level cycleProb≥60, không cạnh/TTL/cooldown)
{
  const active: number[] = [];
  for (let i = WARMUP; i < N; i++) {
    if (!eligible(i)) continue;
    const v = effProb(i, "cycle");
    if (v !== null && v >= 60) active.push(i);
  }
  console.log("=== HIỆN TRẠNG (reference, không vào ranking) ===");
  console.log(`level cycleProb≥60: active=${active.length} ngày · coverage gộp=${coverage(active).toFixed(0)}% · precision=${precision(active).toFixed(0)}%`);
}

// ---- cổng + ranking
const bTrain = 100 * winRate(baseRets.train), bTest = 100 * winRate(baseRets.test);
console.log(`\nBaseline win6T: train=${bTrain.toFixed(0)}% (n=${baseRets.train.length}) · test=${bTest.toFixed(0)}% (n=${baseRets.test.length})`);

const g13 = rows.filter(
  (r) =>
    r.win.train > bTrain && r.win.test > bTest && // G1
    r.covAll >= 35 && r.actPerYear <= 25 && r.yearsFired / r.years >= 0.6 && // G3
    r.n.train >= 5 && r.n.test >= 5 // đủ đợt tối thiểu để so sánh có nghĩa
);
g13.sort((a, b) => b.minExcess - a.minExcess);
console.log(`\nQua G1+G3: ${g13.length}/${rows.length} cấu hình. Chạy placebo (G2) cho top 40…`);

const passed: (Row & { p90: Record<"train" | "test", number> })[] = [];
for (const r of g13.slice(0, 40)) {
  const p90 = {
    train: placeboP90(r.trigsWithRet, "train"),
    test: placeboP90(r.trigsWithRet, "test"),
  };
  if (r.win.train >= p90.train && r.win.test >= p90.test) passed.push({ ...r, p90 });
}

console.log(`\n=== QUA CẢ 4 CỔNG: ${passed.length} cấu hình (xếp min-excess) ===`);
console.log(`key · đợt(n tr/te) · active/năm · năm bắn · cov gộp(cyc/sw) · prec · win6T tr/te (base ${bTrain.toFixed(0)}/${bTest.toFixed(0)}) · med tr/te · p90 placebo tr/te`);
for (const r of passed.slice(0, 15)) {
  console.log(
    `${r.key.padEnd(24)} ${String(r.trigN).padStart(3)}(${r.n.train}/${r.n.test}) · ${r.actPerYear.toFixed(1).padStart(5)}/năm · ${r.yearsFired}/${r.years} · ` +
    `${r.covAll.toFixed(0)}%(${r.covCycle.toFixed(0)}/${r.covSwing.toFixed(0)}) · prec ${r.prec.toFixed(0)}% · ` +
    `win ${r.win.train.toFixed(0)}/${r.win.test.toFixed(0)} · med ${r.med.train.toFixed(1)}/${r.med.test.toFixed(1)} · p90 ${r.p90.train.toFixed(0)}/${r.p90.test.toFixed(0)}`
  );
}
if (passed.length) {
  const w = passed[0];
  const [cand] = [CANDS.find((c) => c.name === w.cand)!];
  console.log(`\nWINNER ${w.key} — JSON cho GOMRAI_CONFIG:`);
  console.log(JSON.stringify({
    binCycleEdge: cand.name.includes("bin-cycle") || cand.name.includes("union") || cand.name.startsWith("hybrid"),
    binSwingEdge: cand.name.includes("bin-swing") || cand.name.includes("union") || cand.name.startsWith("hybrid"),
    probTier: cand.name.startsWith("prob-") ? cand.name.split("-")[1] : cand.name.startsWith("hybrid") ? "max" : null,
    probThreshold: cand.name.startsWith("prob-") || cand.name.startsWith("hybrid") ? Number(cand.name.split("-").pop()) : null,
    ttlSessions: w.ttl, cooldownSessions: w.cool,
    evidence: {
      episodes: w.trigN, win6TTrain: +w.win.train.toFixed(1), win6TTest: +w.win.test.toFixed(1),
      baselineTrain: +bTrain.toFixed(1), baselineTest: +bTest.toFixed(1),
      coveragePct: +w.covAll.toFixed(0), precisionPct: +w.prec.toFixed(0),
      activeDaysPerYear: +w.actPerYear.toFixed(1),
      placeboP90Train: +w.p90.train.toFixed(1), placeboP90Test: +w.p90.test.toFixed(1),
    },
  }, null, 2));
  console.log("\nPHÁN QUYẾT: GO");
} else {
  console.log("\nPHÁN QUYẾT: NO-GO — không cấu hình nào qua cả 4 cổng ⇒ xóa cờ khỏi UI (spec).");
}
```

- [ ] **Step 2: Chạy study**

Run: `npx tsx scripts/gomrai-study.ts`
Expected: chạy xong < ~1 phút, in reference + baseline + bảng + PHÁN QUYẾT.

- [ ] **Step 3: Sanity checklist**

1. Dòng reference "level cycleProb≥60" phải ra active ≈ 380–430 ngày, coverage ≈ 18–22%, precision ≈ 40% (khớp đo tay 2026-07-06; lệch nhỏ do WARMUP là chấp nhận được — lệch lớn = bug).
2. `bin-cycle/ttl0/cd0` phải ra ~5–7 trigger/năm, bắn ~15/17 năm (khớp evidence `bottomStartIdxs` trong docs/bottom.md: "thưa ~6/năm, bắn mọi năm").
3. Baseline test cao hơn train (bull 2019–2026) — nếu ngược = bug era split.
4. Không config nào có `trigN` = 0 mà vẫn vào rows.

Nếu sanity fail: sửa bug trước khi đọc kết quả. Sẵn sàng commit (KHÔNG tự commit).

---

### Task 2: CHECKPOINT — đọc kết quả với chủ dự án, chốt GO/NO-GO

**Files:** không sửa file.

- [ ] **Step 1:** Tóm tắt output study cho chủ dự án: hiện trạng vs top cấu hình (bảng), hoặc NO-GO. Nêu rõ trade-off của winner (đợt/năm, coverage, thinness của n).
- [ ] **Step 2:** Chủ dự án chốt: **GO với config X** (ghi JSON WINNER vào plan/notes) → làm Task 3–7, bỏ Task 8. Hoặc **NO-GO** → bỏ Task 3–7, làm Task 8.
- [ ] **Step 3:** Nếu G3 (coverage 35 / active 25 / years 60%) loại sạch mọi config trong khi G1+G2 có config qua: trình chủ dự án nới/siết ràng buộc (spec cho phép chỉnh sau khi thấy phân bố thực) — KHÔNG tự nới.

**DỪNG Ở ĐÂY chờ quyết định. Các task sau chỉ chạy đúng nhánh đã chốt.**

---

### Task 3 (GO): `GOMRAI_CONFIG` + hàm thuần edge/TTL/cooldown (TDD)

**Files:**
- Modify: `src/lib/types.ts` (thêm `GomRaiConfig` + `GOMRAI_CONFIG`, cạnh `BOTTOM_CONFIG` ~dòng 528)
- Modify: `src/lib/timeline.ts:100-144` (thay ruột `gomRaiIdxs`/`gomRaiIdxsBy`, thêm `gomRaiWindow`, `gomRaiStateAt`)
- Test: `tests/dca-band.test.ts` (viết lại phần gom rải)

**Interfaces:**
- Produces (Task 4–6 dùng):
  - `GOMRAI_CONFIG: GomRaiConfig` (types.ts)
  - `gomRaiWindow(points, isHeadwind, phases, cfg?): { triggers: number[]; inWindow: boolean[] }` — cửa sổ gom thuần (KHÔNG loại ngày buy — buy+cửa sổ = level "strong")
  - `gomRaiIdxsBy(points, isBuy, isHeadwind, phases, cfg?): number[]` — GIỮ signature cũ, giờ = `inWindow[i] && !isBuy[i] && !isHeadwind[i]` (≡ guidance level `dca` từng ngày)
  - `gomRaiIdxs(points, comps, buyThreshold, phases, cfg?)` — wrapper giữ nguyên
  - `gomRaiStateAt(points, isHeadwind, phases, idx, cfg?): { inWindow: boolean; triggerDate: string | null; remaining: number }` — cho text card ("kích hoạt ngày X, còn N phiên")

- [ ] **Step 1: Thêm type + config vào `src/lib/types.ts`** (giá trị = JSON WINNER từ Task 2 — dưới đây là ví dụ shape, PHẢI thay bằng số thật):

```ts
/** Cấu hình cờ "Gom rải" v2 — trigger cạnh lên + TTL + cooldown, tuyển chọn bởi
 *  scripts/gomrai-study.ts (cổng 2 giai đoạn + placebo đồng-n; evidence phải khớp
 *  docs/bottom.md mục "Gom rải v2" — cùng quy ước PRESETS ↔ docs/presets.md). */
export interface GomRaiConfig {
  /** dùng cạnh lên cycleBin==3 làm nguồn trigger */
  binCycleEdge: boolean;
  /** dùng cạnh lên swingBin==3 làm nguồn trigger */
  binSwingEdge: boolean;
  /** nguồn cạnh prob (null = không dùng); "max" = max(cycle,swing) */
  probTier: "cycle" | "swing" | "max" | null;
  probThreshold: number | null;
  /** cửa sổ active sau ngày kích hoạt (0 = chỉ ngày kích hoạt) */
  ttlSessions: number;
  /** khoảng nghỉ tối thiểu giữa 2 lần kích hoạt */
  cooldownSessions: number;
  evidence: {
    episodes: number; win6TTrain: number; win6TTest: number;
    baselineTrain: number; baselineTest: number;
    coveragePct: number; precisionPct: number; activeDaysPerYear: number;
    placeboP90Train: number; placeboP90Test: number;
  };
}
export const GOMRAI_CONFIG: GomRaiConfig = {
  // ==== THAY BẰNG JSON WINNER TỪ scripts/gomrai-study.ts (Task 2) ====
  binCycleEdge: true, binSwingEdge: true, probTier: null, probThreshold: null,
  ttlSessions: 5, cooldownSessions: 21,
  evidence: { episodes: 0, win6TTrain: 0, win6TTest: 0, baselineTrain: 0, baselineTest: 0, coveragePct: 0, precisionPct: 0, activeDaysPerYear: 0, placeboP90Train: 0, placeboP90Test: 0 },
};
```

- [ ] **Step 2: Viết test fail** — thay toàn bộ 2 describe `gomRaiIdxs`/`gomRaiIdxsBy — chế độ đồng thuận preset` trong `tests/dca-band.test.ts` (GIỮ describe `idxRuns`):

```ts
import { describe, it, expect } from "vitest";
import { gomRaiIdxs, gomRaiIdxsBy, gomRaiWindow, gomRaiStateAt, idxRuns } from "../src/lib/timeline";
import { deriveGuidance } from "../src/lib/guidance";
import { zoneOf, type TimelinePoint, type BearPhase, type GomRaiConfig } from "../src/lib/types";

/** Config test cố định — phủ đủ machinery (bin edge + prob edge + TTL + cooldown),
 *  KHÔNG phụ thuộc GOMRAI_CONFIG thật (config thật được khóa bằng test data Task 7). */
const CFG: GomRaiConfig = {
  binCycleEdge: true, binSwingEdge: false, probTier: "max", probThreshold: 60,
  ttlSessions: 2, cooldownSessions: 3,
  evidence: { episodes: 0, win6TTrain: 0, win6TTest: 0, baselineTrain: 0, baselineTest: 0, coveragePct: 0, precisionPct: 0, activeDaysPerYear: 0, placeboP90Train: 0, placeboP90Test: 0 },
};

function pt(d: Partial<TimelinePoint>): TimelinePoint {
  return {
    date: "2020-01-01", price: 1500, composite: 0, zone: "neutral",
    scores: {}, returns: { "21": null, "63": null, "126": null }, ...d,
  } as TimelinePoint;
}
const G = (bins: (number | undefined)[], probs: (number | null)[] = bins.map(() => null)) =>
  bins.map((b, i) => pt({ cycleBin: b, cycleProb: probs[i], cycleN: probs[i] == null ? 0 : 600 }));
const noHead = (n: number) => Array(n).fill(false);
const grind = (n: number): BearPhase[] => Array(n).fill("grind");

describe("gomRaiWindow — cạnh lên + TTL + cooldown", () => {
  it("cạnh lên bin: chỉ ngày chuyển !=3 → 3 kích hoạt; TTL mở cửa sổ", () => {
    // bin: 2 2 3 3 2 — cạnh tại i=2; TTL=2 ⇒ cửa sổ {2,3,4}
    const pts = G([2, 2, 3, 3, 2]);
    const { triggers, inWindow } = gomRaiWindow(pts, noHead(5), grind(5), CFG);
    expect(triggers).toEqual([2]);
    expect([...inWindow]).toEqual([false, false, true, true, true]);
  });
  it("cooldown chặn kích hoạt lại quá gần; hết cooldown thì cho", () => {
    // cạnh tại 1 và 4 (cách 3 = cooldown ⇒ chặn), cạnh tại 6 (cách 5 > 3 ⇒ cho)
    const pts = G([2, 3, 2, 2, 3, 2, 3, 2]);
    const { triggers } = gomRaiWindow(pts, noHead(8), grind(8), CFG);
    expect(triggers).toEqual([1, 6]);
  });
  it("headwind chặn NGÀY KÍCH HOẠT (không mở đợt trong gió ngược)", () => {
    const pts = G([2, 3, 3, 2]);
    const head = [false, true, false, false]; // đúng ngày cạnh
    const { triggers } = gomRaiWindow(pts, head, grind(4), CFG);
    expect(triggers).toEqual([]);
  });
  it("cạnh prob: vượt ngưỡng lần đầu kích hoạt; cổng acute dùng probUw", () => {
    const pts = [
      pt({ cycleBin: 2, cycleProb: 55, cycleN: 600 }),
      pt({ cycleBin: 2, cycleProb: 65, cycleN: 600 }), // cạnh prob tại i=1
      pt({ cycleBin: 2, cycleProb: 65, cycleN: 600 }),
    ];
    expect(gomRaiWindow(pts, noHead(3), grind(3), CFG).triggers).toEqual([1]);
    // acute ⇒ probUw thấp chặn cạnh
    const acute: BearPhase[] = ["grind", "acute", "grind"];
    const pts2 = [
      pt({ cycleBin: 2, cycleProb: 55, cycleN: 600 }),
      pt({ cycleBin: 2, cycleProb: 65, cycleProbUw: 40, cycleN: 600 }),
      pt({ cycleBin: 2, cycleProb: 65, cycleN: 600 }),
    ];
    expect(gomRaiWindow(pts2, noHead(3), acute, CFG).triggers).toEqual([]);
  });
  it("n<10 ⇒ prob không tin được, không kích hoạt", () => {
    const pts = [pt({ cycleProb: 50, cycleN: 9 }), pt({ cycleProb: 70, cycleN: 9 })];
    expect(gomRaiWindow(pts, noHead(2), grind(2), CFG).triggers).toEqual([]);
  });
});

describe("gomRaiIdxsBy — dải ≡ guidance level 'dca' từng ngày", () => {
  // bin cạnh tại i=1, TTL=2 ⇒ cửa sổ {1,2,3}; i=2 là ngày buy, i=3 headwind
  const pts = G([2, 3, 3, 3, 2]);
  const isBuy = [false, false, true, false, false];
  const isHead = [false, false, false, true, false];
  const ph = grind(5);
  it("loại ngày buy/headwind KHỎI DẢI nhưng cửa sổ vẫn chạy", () => {
    expect(gomRaiIdxsBy(pts, isBuy, isHead, ph, CFG)).toEqual([1]);
    // i=2 buy: không vào dải (guidance sẽ là buy/strong); i=3 headwind: guidance headwind
  });
  it("GOLDEN: trùng từng ngày với deriveGuidance level 'dca'", () => {
    const got = new Set(gomRaiIdxsBy(pts, isBuy, isHead, ph, CFG));
    const { inWindow } = gomRaiWindow(pts, isHead, ph, CFG);
    for (let i = 0; i < pts.length; i++) {
      const g = deriveGuidance({
        zone: isBuy[i] ? "buy" : isHead[i] ? "sell" : "neutral",
        composite: isHead[i] ? -45 : isBuy[i] ? 45 : 0,
        bottom: { high: inWindow[i], verified: true, label: "Săn đáy: (test)." },
        premiumPct: null, premiumP80: null,
      });
      expect(got.has(i), `ca ${i}: dải=${got.has(i)} nhưng guidance=${g.level}`).toBe(g.level === "dca");
    }
  });
  it("gomRaiIdxs wrapper ≡ gomRaiIdxsBy dựng từ comps", () => {
    const comps = [0, 0, 45, -45, 0];
    expect(gomRaiIdxs(pts, comps, 40, ph, CFG)).toEqual(
      gomRaiIdxsBy(pts, comps.map((c) => c >= 40), comps.map((c) => c <= -40), ph, CFG)
    );
  });
});

describe("gomRaiStateAt — trạng thái cho card", () => {
  const pts = G([2, 3, 3, 3, 2]);
  const ph = grind(5);
  it("trong cửa sổ: trả ngày kích hoạt + số phiên còn lại", () => {
    const s = gomRaiStateAt(pts, noHead(5), ph, 2, CFG);
    expect(s).toEqual({ inWindow: true, triggerDate: pts[1].date, remaining: 1 });
  });
  it("ngoài cửa sổ: inWindow=false", () => {
    expect(gomRaiStateAt(pts, noHead(5), ph, 4, CFG).inWindow).toBe(false);
  });
});
```

Lưu ý test tồn tại `zoneOf` import — bỏ nếu không dùng sau khi viết xong.

- [ ] **Step 3: Chạy test, xác nhận fail**

Run: `npx vitest run tests/dca-band.test.ts`
Expected: FAIL — `gomRaiWindow is not exported` / hành vi cũ.

- [ ] **Step 4: Implement trong `src/lib/timeline.ts`** — thay khối dòng 100–144 (giữ `idxRuns`, `bottomStartIdxs`):

```ts
import { GOMRAI_CONFIG, type GomRaiConfig } from "./types"; // gộp vào import types sẵn có

/**
 * Cửa sổ "gom rải" v2 (GOMRAI_CONFIG — tuyển chọn bởi scripts/gomrai-study.ts):
 * trigger = CẠNH LÊN của bất kỳ nguồn nào (bin==3 / prob≥thr) tại ngày không headwind,
 * tôn trọng cooldown; cửa sổ active = [t, t+ttl]. KHÔNG loại ngày buy ở đây —
 * buy + cửa sổ = guidance "strong"; dải dca loại buy/headwind ở gomRaiIdxsBy.
 * Chính sách giữ nguyên: pha "acute" ⇒ prob rớt về probUnweighted; n<10 ⇒ bỏ prob.
 */
export function gomRaiWindow(
  points: TimelinePoint[],
  isHeadwind: boolean[],
  phases: BearPhase[],
  cfg: GomRaiConfig = GOMRAI_CONFIG
): { triggers: number[]; inWindow: boolean[] } {
  const n = points.length;
  const effProb = (i: number, tier: "cycle" | "swing"): number | null => {
    const p = points[i];
    const rec = tier === "cycle" ? p.cycleProb ?? null : p.swingProb ?? null;
    const uw = tier === "cycle" ? p.cycleProbUw ?? null : p.swingProbUw ?? null;
    const cnt = tier === "cycle" ? p.cycleN ?? 0 : p.swingN ?? 0;
    if (cnt < 10) return null;
    return phases[i] === "acute" ? uw ?? rec : rec;
  };
  const srcs: ((i: number) => boolean)[] = [];
  if (cfg.binCycleEdge) srcs.push((i) => i >= 0 && points[i].cycleBin === 3);
  if (cfg.binSwingEdge) srcs.push((i) => i >= 0 && points[i].swingBin === 3);
  if (cfg.probTier !== null && cfg.probThreshold !== null) {
    const tier = cfg.probTier;
    srcs.push((i) => {
      if (i < 0) return false;
      const v =
        tier === "max"
          ? Math.max(effProb(i, "cycle") ?? -1, effProb(i, "swing") ?? -1)
          : effProb(i, tier) ?? -1;
      return v >= (cfg.probThreshold as number);
    });
  }
  const triggers: number[] = [];
  const inWindow: boolean[] = new Array(n).fill(false);
  let last = -Infinity;
  for (let i = 0; i < n; i++) {
    if (isHeadwind[i]) continue;
    if (i - last <= cfg.cooldownSessions) continue;
    if (srcs.some((s) => s(i) && !s(i - 1))) {
      triggers.push(i);
      last = i;
      for (let j = i; j <= Math.min(n - 1, i + cfg.ttlSessions); j++) inWindow[j] = true;
    }
  }
  return { triggers, inWindow };
}

/**
 * Các ngày DẢI "gom rải" của Máy thời gian — PHẢI trùng TỪNG NGÀY với
 * deriveGuidance(...).level === "dca" (golden-tested, tests/dca-band.test.ts):
 * trong cửa sổ gomRaiWindow VÀ điểm mua trung tính (không buy, không headwind).
 * Nếu điều kiện của histGuidance đổi, hàm này phải đổi theo (bài học ×0.25 vs ×1.0).
 */
export function gomRaiIdxsBy(
  points: TimelinePoint[],
  isBuy: boolean[],
  isHeadwind: boolean[],
  phases: BearPhase[],
  cfg: GomRaiConfig = GOMRAI_CONFIG
): number[] {
  const { inWindow } = gomRaiWindow(points, isHeadwind, phases, cfg);
  const out: number[] = [];
  for (let i = 0; i < points.length; i++) {
    if (inWindow[i] && !isBuy[i] && !isHeadwind[i]) out.push(i);
  }
  return out;
}

export function gomRaiIdxs(
  points: TimelinePoint[],
  comps: number[],
  buyThreshold: number,
  phases: BearPhase[],
  cfg: GomRaiConfig = GOMRAI_CONFIG
): number[] {
  return gomRaiIdxsBy(
    points,
    comps.map((c) => c >= buyThreshold),
    comps.map((c) => c <= -40),
    phases,
    cfg
  );
}

/** Trạng thái cửa sổ gom tại 1 ngày — cho text card ("kích hoạt ngày X, còn N phiên"). */
export function gomRaiStateAt(
  points: TimelinePoint[],
  isHeadwind: boolean[],
  phases: BearPhase[],
  idx: number,
  cfg: GomRaiConfig = GOMRAI_CONFIG
): { inWindow: boolean; triggerDate: string | null; remaining: number } {
  const { triggers, inWindow } = gomRaiWindow(points, isHeadwind, phases, cfg);
  if (!inWindow[idx]) return { inWindow: false, triggerDate: null, remaining: 0 };
  const t = [...triggers].reverse().find((i) => i <= idx)!;
  return { inWindow: true, triggerDate: points[t].date, remaining: t + cfg.ttlSessions - idx };
}
```

Lưu ý chủ ý: `gomRaiWindow` KHÔNG có cổng warmup — dải hiển thị cả lịch sử sớm (2009–2012) dù evidence chỉ đo từ WARMUP=756; nguồn prob tự chặn sớm bằng `n<10`, nguồn bin có thể bắn sớm — chấp nhận được (cờ cũ cũng hiển thị cả lịch sử), test data Task 7 chỉ khóa từ 2024.

- [ ] **Step 5: Chạy test, xác nhận pass**

Run: `npx vitest run tests/dca-band.test.ts`
Expected: PASS toàn bộ.

- [ ] **Step 6:** `npx vitest run` toàn bộ — các test khác không được vỡ (test nào import `gomRaiIdxsBy` với semantics cũ thì sửa theo semantics mới). Sẵn sàng commit.

---

### Task 4 (GO): Cập nhật `deriveGuidance` doc + copy level `dca`

**Files:**
- Modify: `src/lib/guidance.ts:22-34` (doc `BottomDescriptor`), `src/lib/guidance.ts:159-167` (copy `dca`)

**Interfaces:**
- Consumes: không đổi signature — chỉ đổi Ý NGHĨA `bottom.high` (= "trong cửa sổ gom v2") và chuỗi hiển thị.
- Produces: `deriveGuidance` giữ nguyên matrix; Task 5/6 dựng `bottom.high` từ `gomRaiWindow`/`gomRaiStateAt`.

- [ ] **Step 1:** Sửa doc comment `BottomDescriptor` (dòng 22–26) thành:

```ts
/**
 * Mô tả tín hiệu đáy do caller tự dựng — v2 (GOMRAI_CONFIG): high = ngày đang nằm
 * trong CỬA SỔ GOM (cạnh lên bin/prob + TTL, xem gomRaiWindow trong timeline.ts),
 * không còn là mức prob tuyệt đối ≥60 (đã đo: quá dày + bỏ đáy — docs/bottom.md
 * "Gom rải v2"). Cả live (Dashboard) lẫn lịch sử (Time Machine) dựng CÙNG một cách.
 */
```

- [ ] **Step 2:** Sửa copy nhánh `dca` (dòng 161–166) — `when`/`how` nói theo "đợt":

```ts
  if (!isBuy && bottomHigh) {
    return {
      level: "dca",
      tone: "buy",
      when: "Cửa sổ gom rải đang mở (điểm mua chưa xác nhận)",
      how: "Gom RẢI (DCA) từng phần nhỏ trong cửa sổ này, giữ phần lớn vốn để bồi khi điểm mua xác nhận xu hướng. Đây là điểm dò đáy sớm, không phải lời hứa đáy — dao có thể còn rơi.",
      reasons,
    };
  }
```

- [ ] **Step 3:** `npx vitest run tests/guidance.test.ts tests/dca-band.test.ts` — Expected: PASS (nếu guidance.test.ts assert chuỗi `when`/`how` cũ thì cập nhật chuỗi theo trên). Sẵn sàng commit.

---

### Task 5 (GO): Dashboard live đọc cửa sổ gom từ timeline (card ≡ chart)

**Files:**
- Modify: `src/components/Dashboard.tsx:210-244` (guidance memo) + import

**Interfaces:**
- Consumes: `gomRaiStateAt`, `composites`/`presetComposites` (timeline.ts), `bearPhases` (bear-dca.ts), prop `timeline` (đã có).

- [ ] **Step 1:** Thêm import: `gomRaiStateAt, composites, presetComposites` từ `@/lib/timeline`; `bearPhases` từ `@/lib/bear-dca` (nếu chưa có).

- [ ] **Step 2:** Thêm memo TRƯỚC `guidance` (sau `effProb`, ~dòng 214) — dựng input y hệt Time Machine để 2 nơi khớp từng ngày:

```tsx
  // Cửa sổ "gom rải" v2 — tính từ timeline (CÙNG hàm với dải Time Machine, luật
  // chart ≡ card). Trục headwind = radar composite theo mode hiện tại.
  const gomRai = useMemo(() => {
    const pts = timeline.points;
    const tlComps = preset ? presetComposites(pts, preset) : composites(pts, weights);
    const isHead = tlComps.map((c) => c <= -40);
    const tlPhases = bearPhases(pts.map((q) => q.price));
    return gomRaiStateAt(pts, isHead, tlPhases, pts.length - 1);
  }, [timeline, preset, weights]);
```

- [ ] **Step 3:** Trong `guidance` memo (dòng 232–243): thay `high: best >= 60` bằng `high: gomRai.inWindow`, và label:

```tsx
      bottom: {
        high: gomRai.inWindow,
        verified: cycleVerified || swingVerified,
        label: gomRai.inWindow
          ? `Săn đáy: cửa sổ gom đang mở (kích hoạt ${gomRai.triggerDate}, còn ${gomRai.remaining} phiên) — xác suất gần đáy chu kỳ ${fmt1(effProb.cycle)}%, sóng ${fmt1(effProb.swing)}%${bottomCrashMode ? " (đang sụp cấp tính — ước lượng thận trọng)" : ""}.`
          : `Săn đáy: xác suất gần đáy ${lvl} (chu kỳ ${fmt1(effProb.cycle)}%, sóng ${fmt1(effProb.swing)}%${bottomCrashMode ? " — đang sụp cấp tính, dùng ước lượng thận trọng" : ""}).`,
      },
```

Thêm `gomRai` vào dependency array của `guidance`. `nearBottomLabel` (hero) GIỮ prob-based — đó là ngữ cảnh gauge, không phải cờ.

- [ ] **Step 4:** `npm run build` — Expected: build sạch. Kiểm tra bằng `npx tsx scripts/check-modes.ts` không vỡ. Sẵn sàng commit.

---

### Task 6 (GO): Time Machine — dải mới + marker kích hoạt + histGuidance

**Files:**
- Modify: `src/components/TimeMachine.tsx:190-206` (dcaRuns → thêm window/trigger), `:346-379` (histGuidance), phần render spark (~381-430) + panel ngày

- [ ] **Step 1:** Sau `phases` memo (dòng 192), thay khối `dcaRuns` (193–206):

```tsx
  // v2: trục headwind của cửa sổ = radar composite ≤ −40 (cùng mọi mode).
  const isHead = useMemo(() => comps.map((c) => c <= -40), [comps]);
  const gomWin = useMemo(
    () => gomRaiWindow(points, isHead, phases),
    [points, isHead, phases]
  );
  const dcaRuns = useMemo(
    () =>
      idxRuns(
        buyKs
          ? gomRaiIdxsBy(points, buyKs.map((k) => k >= 1), isHead, phases)
          : gomRaiIdxs(points, comps, buyThr, phases)
      ),
    [points, comps, buyThr, phases, buyKs, isHead]
  );
```

(import thêm `gomRaiWindow` từ `@/lib/timeline`.)

- [ ] **Step 2:** histGuidance (dòng 349–379): thay cách dựng `high`:

```tsx
  const gomState = useMemo(
    () => gomRaiStateAt(points, isHead, phases, idx),
    [points, isHead, phases, idx]
  );
```

Trong `deriveGuidance` call: `high: gomState.inWindow`, label:

```tsx
        label: gomState.inWindow
          ? `Săn đáy: cửa sổ gom đang mở (kích hoạt ${fmtDate(gomState.triggerDate!)}, còn ${gomState.remaining} phiên).`
          : verified
            ? `Săn đáy: xác suất gần đáy ${Math.round(prob)}%${ciStr}${bottomCrashDay ? " (đang sụp cấp tính — ước lượng thận trọng)" : ""}.`
            : "Săn đáy: chưa đủ dữ liệu kiểm chứng.",
```

(giữ `verified` như cũ — vẫn từ prob/n; thêm `gomState` vào deps; import `gomRaiStateAt`.)

- [ ] **Step 3:** Spark: giữ `dcaBands` như cũ (giờ tự thưa/ngắn vì dải mới); thêm marker ngày kích hoạt (chỉ khi `showDca`), cùng pipeline `toMarkers`:

```tsx
    const trigMarkers = showDca ? toMarkers(gomWin.triggers) : [];
```

Render trong SVG cạnh `startMarkers` (hình thoi nhỏ, dùng cùng style class với marker bottomStart nhưng màu của dải dca — UI tối giản, không thêm toggle mới). Panel ngày: khi `gomState.inWindow`, dòng trạng thái "🧺 Cửa sổ gom rải (kích hoạt DD/MM, còn N phiên)".

- [ ] **Step 4:** Chạy `npm run build` + mở nhanh bằng `npm run dev` xem Time Machine: dải thưa hơn hẳn, marker tại ngày kích hoạt, panel ngày khớp dải (chọn 1 ngày trong dải → card GOM RẢI; ngoài dải → không). Sẵn sàng commit.

---

### Task 7 (GO): Test khóa data thật + ràng buộc mật độ

**Files:**
- Create: `tests/gomrai-data.test.ts`

Quá khứ walk-forward là bất biến (prob as-of không đổi khi thêm data mới) ⇒ snapshot trigger TRƯỚC một mốc cố định sẽ ổn định dù cron commit data mới mỗi ngày.

- [ ] **Step 1: Viết test**

```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { gomRaiWindow } from "../src/lib/timeline";
import { bearPhases } from "../src/lib/bear-dca";
import { GOMRAI_CONFIG, type TimelinePoint } from "../src/lib/types";

const tl = JSON.parse(readFileSync("public/data/timeline.json", "utf8"));
const points: TimelinePoint[] = tl.points;
const phases = bearPhases(points.map((p) => p.price));
// trục headwind của cửa sổ = radar composite (trường composite có sẵn trong timeline)
const isHead = points.map((p) => p.composite <= -40);
const CUTOFF = "2026-06-30"; // quá khứ bất biến — chỉ snapshot trước mốc này

describe("Gom rải v2 trên data commit", () => {
  const { triggers, inWindow } = gomRaiWindow(points, isHead, phases);
  it("SNAPSHOT: ngày kích hoạt 2024-01-01..2026-06-30 khớp study (điền từ output Task 1)", () => {
    const got = triggers
      .map((i) => points[i].date)
      .filter((d) => d >= "2024-01-01" && d <= CUTOFF);
    // ==== DÁN danh sách ngày từ output study cho config WINNER ====
    expect(got).toEqual([/* điền khi có kết quả study */]);
  });
  it("ràng buộc mật độ: ngày active/năm ≤ 25 (tính trước cutoff, sau warmup 756)", () => {
    const act = points.filter((p, i) => inWindow[i] && i >= 756 && p.date <= CUTOFF).length;
    const years =
      Number(CUTOFF.slice(0, 4)) - Number(points[756].date.slice(0, 4)) + 1;
    expect(act / years).toBeLessThanOrEqual(25);
  });
  it("evidence trong GOMRAI_CONFIG đã được điền (khác 0)", () => {
    expect(GOMRAI_CONFIG.evidence.episodes).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2:** Chạy study lại lấy danh sách ngày kích hoạt 2024→cutoff của WINNER (thêm dòng print vào study nếu cần), dán vào snapshot.
- [ ] **Step 3:** `npx vitest run` toàn bộ — PASS. Sẵn sàng commit.

---

### Task 8 (chỉ khi NO-GO): gỡ cờ khỏi UI

**Files:**
- Modify: `src/lib/guidance.ts` (bỏ nhánh `dca`; GIỮ `"dca"` trong union type + comment "không còn bắn — NO-GO 2026-07, docs/bottom.md" để khỏi đụng mọi switch/label map)
- Modify: `src/components/TimeMachine.tsx` (bỏ `showDca`, `dcaRuns`, `dcaBands`, toggle ⚙ liên quan)
- Modify: `src/components/Dashboard.tsx` (bottom.high = false cho matrix — hoặc gọn hơn: giữ nguyên input, vì bỏ nhánh dca thì `!isBuy && bottomHigh` rơi xuống `wait`; `strong` GIỮ nguyên prob-based)
- Modify: `src/lib/timeline.ts` (xóa `gomRaiIdxs`/`gomRaiIdxsBy` + comment), `tests/dca-band.test.ts` (chỉ còn `idxRuns`)

- [ ] **Step 1:** guidance.ts — xóa block `if (!isBuy && bottomHigh) {…dca…}` (dòng 159–167); nhánh này rơi xuống `wait`. `strong` (isBuy && bottomHigh) GIỮ — bottomHigh vẫn dựng prob-based như hiện tại ở cả 2 caller.
- [ ] **Step 2:** TimeMachine — xóa state `showDca`, memo `dcaRuns`, render `dcaBands`, mục toggle; Dashboard giữ nguyên (nhánh dca chết tự nhiên).
- [ ] **Step 3:** timeline.ts — xóa `gomRaiIdxs`, `gomRaiIdxsBy`; dca-band.test.ts chỉ giữ describe `idxRuns` (đổi tên file thành `tests/idx-runs.test.ts`).
- [ ] **Step 4:** `npx vitest run && npm run build` — PASS/sạch. Sẵn sàng commit.

---

### Task 9: Docs sync (cả 2 nhánh)

**Files:**
- Modify: `docs/bottom.md` (mục mới "Gom rải v2" hoặc mục NO-GO trong danh sách đã loại)
- Modify: `CLAUDE.md` (đoạn Bottom Hunter — câu về cờ "Gom rải" prob≥60)

- [ ] **Step 1 (GO):** docs/bottom.md thêm mục "## Gom rải v2 (2026-07)": vấn đề đo được (430 ngày/48 đợt, coverage 18%, precision 40%), phương pháp study (grid + 4 cổng), bảng kết quả winner vs hiện trạng vs bin-cycle reference (copy số từ output), lệnh tái lập `npx tsx scripts/gomrai-study.ts`, caveat trung thực (n đợt mỏng nếu <30; coverage đo trên ground-truth nhìn-tương-lai — chỉ dùng tuyển chọn). Ghi rõ evidence phải khớp `GOMRAI_CONFIG`.
- [ ] **Step 2 (NO-GO thay thế):** thêm dòng vào danh sách "Tested and REJECTED" của docs/bottom.md + CLAUDE.md: cờ Gom rải mọi biến thể trigger đều không qua cổng (kèm bảng tóm tắt), cờ đã gỡ khỏi UI ngày 2026-07.
- [ ] **Step 3:** CLAUDE.md: cập nhật câu «"Gom rải" prob≥60 … viable … 7 episodes» theo kết quả thật (GO: mô tả trigger mới + con số; NO-GO: chuyển sang REJECTED). Đồng bộ câu Time Machine "Gợi ý Gom rải bật khi cycle prob ≥ 60" trong docs/bottom.md (mục walk-forward).
- [ ] **Step 4:** Đọc lại docs/bottom.md một lượt xem còn chỗ nào nhắc "prob ≥ 60" như cờ hiện hành.

---

### Task 10: Verification cuối

- [ ] **Step 1:** `npx vitest run` — toàn bộ PASS.
- [ ] **Step 2:** `npm run build` — sạch.
- [ ] **Step 3:** `npx tsx scripts/check-modes.ts` — chạy được, composite các mode không đổi (lớp này không đụng scoring).
- [ ] **Step 4:** `npm run dev` + mở app: card Gợi ý hành động và Time Machine cùng ngày phải khớp (chọn ngày trong/ngoài dải). GO: dải thưa rõ so trước; NO-GO: dải biến mất, gauge còn.
- [ ] **Step 5:** Báo chủ dự án: kết quả + danh sách file đổi + "sẵn sàng commit" (KHÔNG tự commit).
